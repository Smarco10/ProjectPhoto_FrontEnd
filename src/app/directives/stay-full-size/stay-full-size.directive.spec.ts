import { StayFullSizeDirective } from './stay-full-size.directive';

describe('StayFullSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new StayFullSizeDirective();
    expect(directive).toBeTruthy();
  });
});
